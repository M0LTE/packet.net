; ===========================================================================
; Minimal XRouter config for Packet.NET interop tests.
;
; Exposes:
;   - Telnet on :23 (mapped to host :8023)
;   - JSON API on :8086
;   - AXUDP listener on :8095
; ===========================================================================

CALL=PN0XRT
ALIAS=PNXRT
LOCATOR=IO91WL
DNS=8.8.8.8

PORT 1
  ID=AXUDP
  PROTOCOL=AXIP
  UDPPORT=8095
  QUALITY=180
ENDPORT
